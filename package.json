{
  "name": "chatmaxxing",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "cdk",
    "lambda",
    "customer-app",
    "agent-app",
    "phone-agent-app",
    "supervisor-app"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "deploy": "cd cdk && npm run deploy:lambda",
    "fund-data": "node scripts/fund-data/export.mjs --out customer-app/public/fund-data"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
